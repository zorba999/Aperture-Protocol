# Copy to .env.local and fill in.
# The deployer key is only used by scripts/, it never reaches the browser bundle.
DEPLOYER_PRIVATE_KEY=0x...

# Set by `npm run deploy:contract`, or paste an existing deployment here.
NEXT_PUBLIC_CONTRACT_ADDRESS=0x...
NEXT_PUBLIC_GENLAYER_NETWORK=testnetBradbury
